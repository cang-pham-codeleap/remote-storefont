{
  "name": "extension-app",
  "version": "0.1.0",
  "description": "Demo extension for remote-ui host app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "serve": "npx serve dist -p 3001 --cors"
  },
  "dependencies": {
    "@remote-ui/core": "^2.2.7"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
